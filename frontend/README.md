## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Build it:
   `docker build -t neuralnet-cv-app:v1 . `
4. Run it:
   `docker run -p 8080:80 neuralnet-cv-app:v1 `
